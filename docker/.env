# Docker compose config
ENV=

COMPOSE_PROJECT_NAME=${PROJECT_NAME}_${APP_NAME}
COMPOSE_PATH_SEPARATOR=:
# COMPOSE_FILE=deploy/docker-compose${ENV}.yaml:deploy/${DEV_DB_TYPE}/docker-compose.yaml
COMPOSE_FILE=deploy/docker-compose${ENV}.yaml:deploy/${DEV_DB_TYPE}/docker-compose${ENV}.yaml

